Open the developer tools and go to <code>Resources</code> -> <code>Local Storage</code> or reload to see the effect.<br><br>

{{file-input importData="importData"}}
<a {{action "exportData"}} role="button">Export</a>

<br>
<br>

<strong>Name:</strong>
<input value={{this.name}} oninput={{action (mut this.name) value="target.value"}} {{on "enter" (action "createProject")}}>
<a {{action "createProject" name}} role="button">Create project</a>

<br>
<br>

<div class="projects layout">
  <div class="projects__list layout__item u-1/4">
    <strong>Projects:</strong><br>

    {{#each model as |project|}}
      <LinkTo @route="projects.project" @model={{project}}>{{project.name}}</LinkTo> -
      <a {{action "deleteProject" project}} role="button">Delete</a>
      <br>
    {{/each}}
  </div>
  <div class="projects__project layout__item u-3/4">
    {{outlet}}
  </div>
</div>
